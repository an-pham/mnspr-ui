import styled from 'styled-components';

export const Layout = styled.div`
  &.board-container__wrapper {
    display: inline;
  }

  &.board-container__wrapper table {
    border-collapse: collapse;
  }
`
